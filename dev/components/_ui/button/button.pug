mixin button-primary(type, text, className, options)
  if type === 'link'
    a.button.button--primary(class=`${className || ''}`, href=options.link)!= text
  else if type === 'popup'
    button.button.button--primary.js-open-popup(class=`${className || ''}`, data-popup=options.popup)!= text
  else if type === 'submit'
    button.button.button--primary(type="submit", class=`${className || ''}`)= text

mixin button-secondary(type, text, className, options)
  if type === 'link'
    a.button.button--secondary(class=`${className || ''}`, href=options.link)= text

mixin button-tertiary(type, text, className, options)
  if type === 'submit'
    button.button.button--tertiary(type="submit", class=`${className || ''}`)= text
  else if type === 'popup'
    button.button.button--tertiary.js-open-popup(class=`${className || ''}`, data-popup=options.popup)!= text
  else if type === 'link'
    a.button.button--tertiary(type="submit", class=`${className || ''}`, href=options.link)= text

mixin button-quoternary(type, text, className, options)
  if type === 'link'
    a.button.button--quoternary(class=`${className || ''}`, href=options.link)= text
  if type === 'submit'
    button.button.button--quoternary(type="submit", class=`${className || ''}`)= text

mixin button-quinary(type, text, className, options)
  if type === 'link'
    a.button.button--quinary(class=`${className || ''}`, href=options.link)= text